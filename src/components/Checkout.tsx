import React from "react";

//REDUX
import { ApplicationState } from "../redux/store";
import { useSelector } from "react-redux";

const Checkout = () => {
  return (
    <div>
      <h1>In the checkout</h1>
    </div>
  );
};

export default Checkout;
